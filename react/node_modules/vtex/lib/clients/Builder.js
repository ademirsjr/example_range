"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const api_1 = require("@vtex/api");
const SECOND = 1000;
const builderHubTimeout = 2 * SECOND;
const builderBaseRoute = `/_v/builder/0`;
const routes = {
    tsConfig: `${builderBaseRoute}/tsconfig`,
    typings: `${builderBaseRoute}/typings`,
};
class Builder extends api_1.Builder {
    constructor() {
        super(...arguments);
        this.builderHubTsConfig = () => {
            return this.http.get(routes.tsConfig, { timeout: builderHubTimeout });
        };
        this.typingsInfo = () => __awaiter(this, void 0, void 0, function* () {
            const res = yield this.http.get(routes.typings, { timeout: builderHubTimeout });
            return res.typingsInfo;
        });
    }
}
exports.default = Builder;
