"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const chalk_1 = require("chalk");
const errors_1 = require("../../errors");
const conf_1 = require("./../../conf");
exports.default = (name) => {
    switch (name) {
        case 'env':
            const value = conf_1.getEnvironment() || '';
            console.log(value);
            break;
        default:
            throw new errors_1.CommandError(`The only supported configuration is: ${chalk_1.default.blue('env')}`);
    }
};
